import { domainAnotherDomain } from './domain-another-domain';

describe('domainAnotherDomain', () => {
  it('should work', () => {
    expect(domainAnotherDomain()).toEqual('domain-another-domain');
  });
});
